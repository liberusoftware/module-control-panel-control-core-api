# Control Panel Control Core API

The API adapter exposes only the approved Control Core node operations under
`/api/v1/control-panel/control-core`. It requires Sanctum authentication and
delegates mutations to the core module.
